import { Lato } from 'next/font/google';

export const fontSans = Lato({
  subsets: ['latin'],
  variable: '--font-sans',
  weight: '400'
});

export const fontMono = Lato({
  subsets: ['latin'],
  variable: '--font-mono',
  weight: '400'
});



